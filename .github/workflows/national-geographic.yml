name: National Geographic

on:
  # schedule:
  #   - cron: "0 0 1 * *"
  workflow_dispatch:

jobs:
  fetch-news:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Calibre
        run: |
          sudo apt-get update
          sudo apt-get install libegl1 libopengl0 libxcb-cursor0
          sudo -v && wget -nv -O- https://download.calibre-ebook.com/linux-installer.sh | sudo sh /dev/stdin

      - name: Set current date in env variables
        run: |
          echo "DATE=$(date +%Y-%m-%d)" >> $GITHUB_ENV

      - name: Fetch news
        run: |
          mkdir -p output
          YEAR=$(date +%Y)
          MONTH=$(date +%m)
          SERIES_INDEX="${YEAR}.${MONTH}"

          ebook-convert "custom-recipes/National Geographic.recipe" "national-geographic-${{ env.DATE }}.kepub" \
            --recipe-specific-option=res:1500 \
            --authors "Various Authors" \
            --author-sort "Various Authors" \
            --tags "News" \
            --publisher "National Geographic" \
            --series "National Geographic" \
            --series-index "$SERIES_INDEX"

      - name: Upload .kepub file to artifacts
        uses: actions/upload-artifact@v4
        with:
          name: national-geographic-${{ env.DATE }}
          path: national-geographic-${{ env.DATE }}.kepub
          retention-days: 90

      - name: Upload .kepub file to VPS
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_KEY }}
          source: national-geographic-${{ env.DATE }}.kepub
          target: ${{ secrets.VPS_PATH }}
